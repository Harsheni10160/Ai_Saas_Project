/** @type {import('next').NextConfig} */
const nextConfig = {
  // Tailwind v4 setup is usually automatic in Next.js 15+ if postcss is configured,
  // but we can try to be explicit if it helps.
};

module.exports = nextConfig;
